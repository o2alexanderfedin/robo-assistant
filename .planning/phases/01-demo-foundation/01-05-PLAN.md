---
phase: 01-demo-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - src/features/chat/hooks/useChatSimulation.ts
  - src/features/chat/components/ChatContainer.tsx
  - src/App.tsx
  - src/components/layout/LeftPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Chat responses use scenario system (not hardcoded)"
    - "User can switch between Startup Founder and Executive personas"
    - "Persona switch changes context and mock data"
    - "Initial chat shows persona-appropriate welcome message"
  artifacts:
    - path: "src/features/chat/hooks/useChatSimulation.ts"
      provides: "Chat simulation using scenario system"
      contains: "getScenarioResponse"
    - path: "src/App.tsx"
      provides: "Persona state management"
      contains: "activePersona"
  key_links:
    - from: "src/features/chat/hooks/useChatSimulation.ts"
      to: "src/features/demo-scenarios/index.ts"
      via: "scenario response integration"
      pattern: "getScenarioResponse|matchScenario"
    - from: "src/App.tsx"
      to: "src/features/personas/data.ts"
      via: "persona data usage"
      pattern: "PERSONAS|activePersona"
---

<objective>
Integrate demo scenarios with chat simulation and add persona switching capability.

Purpose: Connect the chat interface (Plan 03) with the scenario system (Plan 04) to create a cohesive demo experience. Adding persona switching allows demonstrating different use cases to different potential customers.

Output: A complete chat experience where responses follow business scenarios, with ability to switch between Startup Founder and Executive personas.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-foundation/01-CONTEXT.md
@.planning/phases/01-demo-foundation/01-RESEARCH.md
@.planning/phases/01-demo-foundation/01-03-SUMMARY.md
@.planning/phases/01-demo-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate scenario system with chat simulation</name>
  <files>
    src/features/chat/hooks/useChatSimulation.ts
  </files>
  <action>
    Update useChatSimulation.ts to use scenario system:

    Accept persona parameter:
    ```typescript
    export function useChatSimulation(persona: Persona)
    ```

    Update sendMessage function:
    1. Add user message to state immediately
    2. Call `getScenarioResponse(userMessage, persona, messages)`
    3. Determine typing delay based on response length:
       - Short responses (< 50 chars): 800ms
       - Medium responses (50-200 chars): 1500ms
       - Long responses (> 200 chars): 2200ms
    4. Add assistant response to state

    Add initial welcome message on mount:
    - Detect if messages array is empty
    - Generate persona-appropriate greeting via scenario system
    - Include quick orientation: "I can help with [available scenarios]"

    Handle persona changes:
    - Accept reset callback or effect
    - Clear messages and show new welcome when persona changes
    - Use useEffect with persona dependency

    Track scenario state:
    - Current active scenario (if any)
    - Step within scenario
    - Pass to getScenarioResponse for context
  </action>
  <verify>
    Chat responses now use scenario system
    "email" triggers email-triage scenario response
    Off-script messages get redirect response
    Typing delay varies by response length
  </verify>
  <done>
    Chat simulation fully integrated with scenario system, responses are context-aware
  </done>
</task>

<task type="auto">
  <name>Task 2: Add persona switching to App</name>
  <files>
    src/App.tsx
    src/components/layout/LeftPanel.tsx
  </files>
  <action>
    Update App.tsx:
    - Import personas from features/personas/data
    - Add state: `const [activePersona, setActivePersona] = useState(PERSONAS[0])`
    - Pass activePersona to ChatContainer
    - Pass activePersona and setActivePersona to LeftPanel

    Update LeftPanel.tsx:
    - Add persona switcher UI in AssistantStatus section or as new section
    - Display current persona name and title
    - Add switch control (dropdown or toggle)
    - Options: "Alex Chen - Startup Founder" and "Jennifer Walsh - Executive/CEO"
    - On switch: call setActivePersona with new persona
    - Visual indication of current persona (avatar, name, company)

    Consider adding persona context panel:
    - Below assistant status or as expandable section
    - Show current persona's company, priorities
    - Help user understand the demo context
  </action>
  <verify>
    Persona switcher visible in left panel
    Switching persona updates displayed name/context
    Chat clears and shows new welcome message on switch
    Both personas are selectable
  </verify>
  <done>
    Persona switching implemented with UI and state management
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ChatContainer and verify complete flow</name>
  <files>
    src/features/chat/components/ChatContainer.tsx
  </files>
  <action>
    Update ChatContainer.tsx:
    - Accept persona prop: `persona: Persona`
    - Pass persona to useChatSimulation hook
    - Add useEffect to detect persona changes and reset chat
    - Display persona context in chat header:
      - "Assisting [persona.name] at [persona.company]"
      - Small subtitle with persona context

    Add visual feedback for scenario context:
    - When in a scenario, show subtle indicator
    - "Currently helping with: Email Triage" (optional enhancement)

    Ensure smooth transitions:
    - Fade out/in animation on persona switch
    - Welcome message appears after transition
    - No jarring layout shifts

    Test complete flow end-to-end:
    1. App loads -> Startup Founder persona active
    2. Chat shows welcome: "Good morning, Alex! Here's what needs your attention..."
    3. Type "check emails" -> Email triage scenario begins
    4. Switch to Executive persona -> Chat resets
    5. New welcome: "Good morning, Jennifer! Here's your executive briefing..."
    6. Type "schedule meeting" -> Meeting scheduling scenario
  </action>
  <verify>
    npm run dev shows complete working demo
    Persona switch resets chat with appropriate welcome
    Scenarios work correctly for both personas
    UI is polished and responsive
    No TypeScript errors (npx tsc --noEmit)
  </verify>
  <done>
    Complete integration of chat, scenarios, and personas - demo is fully functional
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev`

2. Test Startup Founder flow:
   - Default persona shows Alex Chen context
   - Welcome message is appropriate for startup founder
   - Type "emails" -> email triage with investor emails
   - Type "schedule" -> meeting scheduling

3. Test Executive flow:
   - Switch to Jennifer Walsh
   - Chat resets with new welcome
   - Type "briefing" -> executive daily briefing
   - Type "tasks" -> task management with board-level tasks

4. Test off-script handling:
   - Type random text -> friendly redirect
   - Redirect offers available scenarios

5. Test conversation continuity:
   - Multi-message scenario flow works
   - Scenario progresses through steps

6. Run `npm run lint` - no errors
7. Run `npx tsc --noEmit` - TypeScript passes
</verification>

<success_criteria>
- Chat uses scenario system for all responses
- Persona switcher allows changing between two personas
- Switching personas resets chat with appropriate welcome
- Demo feels cohesive and professional
- All scenarios work correctly with both personas
- Off-script inputs are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-foundation/01-05-SUMMARY.md`
</output>
