---
phase: 01-demo-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/SplitLayout.tsx
  - src/components/layout/LeftPanel.tsx
  - src/components/layout/RightPanel.tsx
  - src/components/layout/ChannelSwitcher.tsx
  - src/components/layout/ConversationList.tsx
  - src/components/layout/QuickActions.tsx
  - src/components/layout/AssistantStatus.tsx
  - src/types/index.ts
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User sees split-view layout with left panel and right panel"
    - "Left panel shows channel switcher, conversation list, quick actions, and assistant status"
    - "Right panel is ready to display active conversation content"
    - "Layout is responsive and fills the viewport"
  artifacts:
    - path: "src/components/layout/SplitLayout.tsx"
      provides: "Main layout container with left/right panels"
      min_lines: 20
    - path: "src/components/layout/LeftPanel.tsx"
      provides: "Navigation panel with four sections"
      min_lines: 30
    - path: "src/components/layout/RightPanel.tsx"
      provides: "Content area for active conversation"
      min_lines: 15
    - path: "src/types/index.ts"
      provides: "Shared TypeScript types for channels, conversations"
      contains: "Channel"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/SplitLayout.tsx"
      via: "component import and render"
      pattern: "SplitLayout"
    - from: "src/components/layout/SplitLayout.tsx"
      to: "src/components/layout/LeftPanel.tsx"
      via: "component composition"
      pattern: "LeftPanel"
---

<objective>
Create the foundational split-view layout with navigation panel and content area.

Purpose: Establish the visual structure that users will interact with - a left panel for navigation and context, and a right panel for the active conversation. This layout will persist across all phases.

Output: A responsive split-view layout with placeholder content in all sections, ready to receive chat components.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-foundation/01-CONTEXT.md
@.planning/phases/01-demo-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared types and create layout structure</name>
  <files>
    src/types/index.ts
    src/components/layout/SplitLayout.tsx
  </files>
  <action>
    Create src/types/index.ts with foundational types:
    ```typescript
    export type Channel = 'chat' | 'whatsapp' | 'email' | 'voice';

    export interface Conversation {
      id: string;
      title: string;
      lastMessage: string;
      timestamp: Date;
      unread: boolean;
      channel: Channel;
    }

    export interface Message {
      id: string;
      role: 'user' | 'assistant';
      content: string;
      timestamp: Date;
    }
    ```

    Create src/components/layout/SplitLayout.tsx:
    - Full viewport height container (h-screen)
    - Flexbox row layout
    - Left panel: fixed width (w-80 or 320px), border-right
    - Right panel: flex-1 (fills remaining space)
    - Use Tailwind classes for light & airy styling:
      - bg-background for main container
      - Subtle border between panels
      - Clean, minimal aesthetic
    - Import and render LeftPanel and RightPanel components
  </action>
  <verify>
    TypeScript compilation succeeds (npx tsc --noEmit)
    Component renders without errors in browser
  </verify>
  <done>
    Shared types defined, SplitLayout component renders with proper proportions
  </done>
</task>

<task type="auto">
  <name>Task 2: Create left panel with four sections</name>
  <files>
    src/components/layout/LeftPanel.tsx
    src/components/layout/ChannelSwitcher.tsx
    src/components/layout/ConversationList.tsx
    src/components/layout/QuickActions.tsx
    src/components/layout/AssistantStatus.tsx
  </files>
  <action>
    Create LeftPanel.tsx as the container for navigation:
    - Flexbox column layout (flex-col)
    - Full height of parent
    - Contains four sections in order:
      1. ChannelSwitcher (top)
      2. ConversationList (scrollable middle, flex-1)
      3. QuickActions (above assistant status)
      4. AssistantStatus (bottom)
    - Use subtle dividers or spacing between sections

    Create ChannelSwitcher.tsx:
    - Horizontal row of channel icons/buttons
    - Channels: Chat (default), WhatsApp, Email, Voice
    - Use Lucide React icons: MessageSquare, Phone (for WhatsApp), Mail, Mic
    - Active channel highlighted (bg-primary/10 or similar)
    - Accept activeChannel prop and onChannelChange callback
    - Placeholder for now - just visual, state in parent later

    Create ConversationList.tsx:
    - Scrollable list area
    - Each conversation shows: title, last message preview, timestamp
    - Unread indicator (dot or bold text)
    - Accept conversations array prop
    - Empty state: "No conversations yet"
    - Use mock data for now (2-3 placeholder conversations)

    Create QuickActions.tsx:
    - Row of action buttons
    - Actions: "New Message", "Schedule Meeting", "Create Task"
    - Use icons + text labels
    - Buttons should look clickable but be non-functional for now

    Create AssistantStatus.tsx:
    - Avatar/icon for assistant
    - Status text: "Online" with green dot
    - Brief capabilities hint: "Ready to help with emails, meetings, and tasks"
    - Fixed at bottom of left panel
  </action>
  <verify>
    All components render without errors
    Left panel shows all four sections in correct order
    Scrolling works in conversation list when content overflows
  </verify>
  <done>
    Left panel complete with channel switcher, conversation list, quick actions, and assistant status
  </done>
</task>

<task type="auto">
  <name>Task 3: Create right panel and wire up App.tsx</name>
  <files>
    src/components/layout/RightPanel.tsx
    src/App.tsx
  </files>
  <action>
    Create RightPanel.tsx:
    - Placeholder for conversation content
    - Header area (for conversation title/context)
    - Main content area (where chat will go)
    - Footer area (where input will go)
    - Centered "Select a conversation" message as empty state
    - Accept children prop for future content injection
    - Flexbox column layout (flex-col h-full)

    Add shadcn/ui components needed:
    ```bash
    npx shadcn@latest add button card avatar scroll-area
    ```

    Update src/App.tsx:
    - Import SplitLayout
    - Render SplitLayout as main content
    - Remove default Vite placeholder content
    - Add basic state for activeChannel (useState)
    - Pass activeChannel to relevant components
  </action>
  <verify>
    npm run dev shows complete layout
    Left panel with all sections visible
    Right panel with placeholder content
    No TypeScript errors
  </verify>
  <done>
    Complete layout structure rendered in App, shadcn/ui components installed, ready for chat integration
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev` - should show split layout
2. Left panel visible with all four sections
3. Right panel shows placeholder message
4. Layout fills viewport without scrollbars on body
5. Conversation list scrolls independently if content overflows
6. Run `npm run lint` - no errors
7. Run `npx tsc --noEmit` - TypeScript passes
</verification>

<success_criteria>
- Split-view layout renders with correct proportions
- All four left panel sections visible and properly positioned
- Right panel ready to receive chat content
- Layout is responsive and fills viewport
- Components use consistent light & airy styling
- TypeScript types defined for channels and conversations
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-foundation/01-02-SUMMARY.md`
</output>
