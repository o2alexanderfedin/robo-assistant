---
phase: 01-demo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.node.json
  - vite.config.ts
  - eslint.config.js
  - .prettierrc
  - .gitignore
  - src/main.tsx
  - src/App.tsx
  - src/index.css
  - src/vite-env.d.ts
  - src/lib/utils.ts
  - components.json
  - tailwind.config.js
  - postcss.config.js
autonomous: true

must_haves:
  truths:
    - 'npm run dev starts a development server with hot reload'
    - 'TypeScript strict mode is enabled and catches type errors'
    - 'ESLint + Prettier enforce consistent code style'
    - 'shadcn/ui components can be added via CLI'
    - 'Path alias @/ resolves to src/'
  artifacts:
    - path: 'package.json'
      provides: 'Project dependencies and scripts'
      contains: 'vite'
    - path: 'tsconfig.json'
      provides: 'TypeScript configuration'
      contains: '"strict": true'
    - path: 'vite.config.ts'
      provides: 'Vite build configuration'
      contains: 'alias'
    - path: 'src/lib/utils.ts'
      provides: 'shadcn/ui cn() utility'
      contains: 'twMerge'
    - path: 'components.json'
      provides: 'shadcn/ui configuration'
      contains: 'aliases'
  key_links:
    - from: 'vite.config.ts'
      to: 'tsconfig.json'
      via: 'path alias configuration'
      pattern: '@.*src'
    - from: 'src/index.css'
      to: 'tailwind.config.js'
      via: 'Tailwind directives'
      pattern: '@tailwind'
---

<objective>
Set up the project foundation with Vite, React, TypeScript strict mode, shadcn/ui, and development tooling.

Purpose: Establish a production-quality development environment that enables fast iteration with type safety and consistent code style.

Output: A working React application skeleton that runs via `npm run dev` with hot reload, strict TypeScript, and shadcn/ui ready for component additions.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-foundation/01-CONTEXT.md
@.planning/phases/01-demo-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite + React + TypeScript project</name>
  <files>
    package.json
    tsconfig.json
    tsconfig.node.json
    vite.config.ts
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
    .gitignore
  </files>
  <action>
    Initialize project using Vite React-TS template:
    ```bash
    npm create vite@latest . -- --template react-ts
    npm install
    ```

    Configure vite.config.ts with:
    - Path alias: `@` -> `./src`
    - Base path: `/robo-assistant/` for GitHub Pages
    - React plugin

    Update tsconfig.json to enable strict mode:
    - Set `"strict": true`
    - Add path alias: `"@/*": ["./src/*"]`
    - Add `"baseUrl": "."`

    Update tsconfig.node.json for Vite config file typing.

    Keep App.tsx minimal - just a placeholder div with "Robo Assistant" text.
    Keep main.tsx with standard React 18 createRoot pattern.

  </action>
  <verify>
    npm run dev starts server without errors
    TypeScript compilation succeeds (npx tsc --noEmit)
    Access http://localhost:5173 shows placeholder
  </verify>
  <done>
    Vite dev server runs with hot reload, TypeScript strict mode enabled, path aliases configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shadcn/ui and Tailwind CSS</name>
  <files>
    src/lib/utils.ts
    src/index.css
    components.json
    tailwind.config.js
    postcss.config.js
    package.json
  </files>
  <action>
    Install Tailwind CSS and dependencies:
    ```bash
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    ```

    Initialize shadcn/ui with manual setup (per research):
    ```bash
    npx shadcn@latest init
    ```

    Select options during init:
    - TypeScript: Yes
    - Style: Default
    - Base color: Slate (will customize for light & airy)
    - CSS variables: Yes
    - Path alias: @/components and @/lib
    - React Server Components: No (Vite, not Next.js)

    Create src/lib/utils.ts with cn() utility:
    ```typescript
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    Update src/index.css with Tailwind directives and light & airy theme colors:
    - Use oklch color space for modern, perceptually uniform colors
    - White/light gray backgrounds
    - Soft blue primary color
    - Rounded borders (--radius: 0.75rem)
    - See research document for exact color values

    Import index.css in main.tsx.

  </action>
  <verify>
    npm run dev shows styled page (Tailwind classes apply)
    npx shadcn@latest add button succeeds
    cn() utility imports without errors
  </verify>
  <done>
    shadcn/ui installed with Tailwind CSS, cn() utility available, light theme configured
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure ESLint + Prettier</name>
  <files>
    eslint.config.js
    .prettierrc
    package.json
  </files>
  <action>
    Install ESLint plugins and Prettier:
    ```bash
    npm install -D prettier eslint-plugin-prettier eslint-config-prettier
    ```

    Create eslint.config.js using flat config format (modern):
    - Extend @eslint/js recommended
    - Extend typescript-eslint recommended
    - Add react-hooks plugin (from Vite template)
    - Add react-refresh plugin (from Vite template)
    - Add prettier plugin (must be last)
    - Enable prettier/prettier rule as error
    - See research document for exact config

    Create .prettierrc:
    ```json
    {
      "singleQuote": true,
      "semi": true,
      "tabWidth": 2,
      "trailingComma": "es5",
      "printWidth": 100,
      "bracketSpacing": true,
      "endOfLine": "lf"
    }
    ```

    Add npm scripts to package.json:
    - "lint": "eslint ."
    - "lint:fix": "eslint . --fix"
    - "format": "prettier --write ."

  </action>
  <verify>
    npm run lint runs without configuration errors
    npm run format formats files consistently
    Intentional lint error (unused variable) is caught
  </verify>
  <done>
    ESLint with TypeScript rules and Prettier integration configured, npm scripts available
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev` - should start without errors, show placeholder page
2. Run `npx tsc --noEmit` - TypeScript compilation should pass
3. Run `npm run lint` - should complete without errors
4. Test path alias: create test import `import { cn } from '@/lib/utils'` in App.tsx
5. Verify hot reload: change text in App.tsx, browser should update instantly
   </verification>

<success_criteria>

- Development server starts with `npm run dev`
- TypeScript strict mode catches type errors
- Tailwind classes apply correctly
- ESLint + Prettier enforce code style
- shadcn/ui CLI can add components
- Path alias @/ works in imports
  </success_criteria>

<output>
After completion, create `.planning/phases/01-demo-foundation/01-01-SUMMARY.md`
</output>
