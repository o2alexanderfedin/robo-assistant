---
phase: 01-demo-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/features/chat/components/ChatContainer.tsx
  - src/features/chat/components/MessageList.tsx
  - src/features/chat/components/ChatBubble.tsx
  - src/features/chat/components/ChatInput.tsx
  - src/features/chat/components/TypingIndicator.tsx
  - src/features/chat/hooks/useAutoScroll.ts
  - src/features/chat/hooks/useChatSimulation.ts
  - src/features/chat/types.ts
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User can type a message in the chat input"
    - "User message appears in chat as a bubble"
    - "Assistant response appears after typing indicator"
    - "Chat auto-scrolls to show newest messages"
    - "Messages have distinct styling for user vs assistant"
  artifacts:
    - path: "src/features/chat/components/ChatContainer.tsx"
      provides: "Main chat wrapper integrating all chat components"
      min_lines: 30
    - path: "src/features/chat/components/MessageList.tsx"
      provides: "Scrollable message display with auto-scroll"
      min_lines: 25
    - path: "src/features/chat/components/ChatBubble.tsx"
      provides: "Individual message bubble with role-based styling"
      min_lines: 20
    - path: "src/features/chat/components/ChatInput.tsx"
      provides: "Text input with send button"
      min_lines: 25
    - path: "src/features/chat/hooks/useChatSimulation.ts"
      provides: "Hook managing messages state and simulated responses"
      exports: ["useChatSimulation"]
  key_links:
    - from: "src/features/chat/components/ChatContainer.tsx"
      to: "src/features/chat/hooks/useChatSimulation.ts"
      via: "hook invocation for chat state"
      pattern: "useChatSimulation"
    - from: "src/features/chat/components/ChatInput.tsx"
      to: "src/features/chat/hooks/useChatSimulation.ts"
      via: "sendMessage callback"
      pattern: "onSend|sendMessage"
    - from: "src/App.tsx"
      to: "src/features/chat/components/ChatContainer.tsx"
      via: "component render in RightPanel"
      pattern: "ChatContainer"
---

<objective>
Build the complete chat interface with message display, input, typing indicator, and simulated AI responses.

Purpose: Create the core user interaction - typing messages and receiving AI responses. This is the primary UI-01 requirement (natural language chat interface) that demonstrates the product's value.

Output: A fully functional chat interface where users can send messages and see AI responses with realistic typing delays.
</objective>

<execution_context>
@/Users/alexanderfedin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexanderfedin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-demo-foundation/01-CONTEXT.md
@.planning/phases/01-demo-foundation/01-RESEARCH.md
@.planning/phases/01-demo-foundation/01-01-SUMMARY.md
@.planning/phases/01-demo-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat components (bubbles, list, input)</name>
  <files>
    src/features/chat/types.ts
    src/features/chat/components/ChatBubble.tsx
    src/features/chat/components/MessageList.tsx
    src/features/chat/components/ChatInput.tsx
    src/features/chat/components/TypingIndicator.tsx
  </files>
  <action>
    Create src/features/chat/types.ts extending base Message type:
    ```typescript
    export interface ChatMessage {
      id: string;
      role: 'user' | 'assistant';
      content: string;
      timestamp: Date;
      // Rich content support for Phase 2+
      richContent?: {
        type: 'email-preview' | 'calendar-event' | 'task-card' | 'action-buttons';
        data: unknown;
      };
    }
    ```

    Create ChatBubble.tsx using CVA pattern (from research):
    - Variants for user (right-aligned, primary color) and assistant (left-aligned, muted bg)
    - Rounded corners (rounded-2xl for friendly feel)
    - Max width ~70% of container
    - Display timestamp below message (small, muted text)
    - Use cn() utility for class merging

    Create MessageList.tsx:
    - Render array of ChatMessage using ChatBubble
    - Use shadcn/ui ScrollArea for consistent scrolling
    - Accept messages prop (ChatMessage[])
    - Accept isTyping prop to show TypingIndicator
    - Include invisible anchor div at bottom for auto-scroll
    - Empty state: "Start a conversation by typing a message"

    Create ChatInput.tsx:
    - Text input field (controlled)
    - Send button with icon (SendHorizontal from Lucide)
    - Handle Enter key to submit (Shift+Enter for newline)
    - Disable send when input empty
    - Accept onSend callback prop
    - Clear input after send
    - Light & airy styling: subtle border, rounded, shadow on focus

    Create TypingIndicator.tsx:
    - Three animated dots (bounce animation with staggered delays)
    - "Assistant is typing..." text
    - Matches assistant bubble styling (left-aligned)
    - Use Tailwind animation classes from research doc
  </action>
  <verify>
    Components render without errors (import in App.tsx temporarily)
    TypeScript compilation passes
    Styling matches light & airy design (visual check)
  </verify>
  <done>
    All chat UI components created with proper styling and props interfaces
  </done>
</task>

<task type="auto">
  <name>Task 2: Create hooks for auto-scroll and chat simulation</name>
  <files>
    src/features/chat/hooks/useAutoScroll.ts
    src/features/chat/hooks/useChatSimulation.ts
  </files>
  <action>
    Create useAutoScroll.ts (from research pattern):
    - Accept messages array as dependency
    - Return scrollRef (for scroll container) and anchorRef (for scroll target)
    - Track whether user is at bottom (within 50px threshold)
    - Only auto-scroll if user was already at bottom
    - Use useEffect to scroll when messages change
    - Handle scroll event to update isAtBottom state

    Create useChatSimulation.ts:
    - Manage messages state (useState<ChatMessage[]>)
    - Manage isTyping state (useState<boolean>)
    - Provide sendMessage function:
      1. Add user message immediately to messages array
      2. Set isTyping true
      3. Wait 1-2 seconds (simulate thinking)
      4. Set isTyping false
      5. Add assistant response to messages array
    - For now, use simple response mapping (will enhance in Plan 04):
      - Default response: "I understand you said: '{userMessage}'. I'm ready to help with your emails, meetings, and tasks."
      - Recognize keywords: "email" -> respond about email triage
      - Recognize keywords: "meeting" -> respond about scheduling
    - Return { messages, isTyping, sendMessage }
  </action>
  <verify>
    Hooks can be imported without errors
    TypeScript types are correct
    useChatSimulation returns expected shape
  </verify>
  <done>
    Auto-scroll hook handles scroll behavior, chat simulation hook manages message state and responses
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ChatContainer and integrate in App</name>
  <files>
    src/features/chat/components/ChatContainer.tsx
    src/App.tsx
  </files>
  <action>
    Create ChatContainer.tsx:
    - Use useChatSimulation hook for state
    - Use useAutoScroll hook for scroll behavior
    - Layout: flex column, full height
      - Header: conversation title, assistant avatar (optional for Phase 1)
      - Body: MessageList with scrollRef, flex-1
      - Footer: ChatInput
    - Pass messages and isTyping to MessageList
    - Pass sendMessage to ChatInput
    - Apply scroll refs to appropriate elements

    Update App.tsx:
    - Import ChatContainer
    - Render ChatContainer inside RightPanel
    - Remove placeholder content from RightPanel
    - Keep channel state management (will connect later)

    Create index exports for features/chat:
    - src/features/chat/index.ts exporting ChatContainer

    Test the complete flow:
    - Type message -> appears as user bubble
    - Wait for typing indicator
    - Assistant response appears
    - Chat scrolls to show new message
  </action>
  <verify>
    npm run dev shows working chat interface
    User can type and send messages
    Assistant responses appear with typing delay
    Chat scrolls automatically
    No TypeScript errors (npx tsc --noEmit)
  </verify>
  <done>
    Complete chat interface integrated in App, users can send messages and receive simulated responses
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run `npm run dev`
2. Type a message in the chat input and press Enter
3. User message appears as bubble (right side, primary color)
4. Typing indicator shows ("Assistant is typing...")
5. After 1-2 seconds, assistant response appears (left side, muted color)
6. Send multiple messages rapidly - chat scrolls to show newest
7. Scroll up to read old messages, send new message - should NOT force scroll if reading history
8. Run `npm run lint` - no errors
9. Run `npx tsc --noEmit` - TypeScript passes
</verification>

<success_criteria>
- User can type and send messages
- User messages display with user styling (right, primary)
- Assistant messages display with assistant styling (left, muted)
- Typing indicator shows before assistant response
- Auto-scroll works correctly (respects user scroll position)
- Chat interface fits within right panel layout
- All components properly typed with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-foundation/01-03-SUMMARY.md`
</output>
